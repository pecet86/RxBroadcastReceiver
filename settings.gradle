buildCache {
    local {
        directory = new File(rootDir, '.gradle/build-cache')
        removeUnusedEntriesAfterDays = 30
    }
}

rootProject.name = 'RxJava3 binding for Android BroadcastReceiver'

include ':library'
